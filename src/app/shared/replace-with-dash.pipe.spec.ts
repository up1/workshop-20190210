import { ReplaceWithDashPipe } from './replace-with-dash.pipe';

describe('ReplaceWithDashPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWithDashPipe();
    expect(pipe).toBeTruthy();
  });
});
